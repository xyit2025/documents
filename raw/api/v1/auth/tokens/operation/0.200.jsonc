{
    "success": true, //* 请求业务是否成功
    "code": string, //* 业务码
    "timestamp": string, //* 时间戳，(pattern = "yyyy-MM-dd'T'HH:mm:ss.SSSXXX", timezone = "GMT+8")
    "message": "success", // 响应段消息
    "data": {
        "token": string, //* 获取到的 Token
        "verify_image": string, //* Token 对应的验证图像, 编码 Base64
        "expired_in": int //有效期, 单位 分钟
    }
}